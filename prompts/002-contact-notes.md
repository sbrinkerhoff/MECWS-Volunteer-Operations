# Summary

The `team members` page should contain a `notes` field for each team member. This field should be a textarea that allows for multi-line text input. The `notes` field should be optional and should not be required.  The notes field should only be visible to the `shelter supervisor` role and not visible by the `team member` role.